{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "deploy",
            "type": "shell",
            "command": "docker run --rm -it --mount \"type=bind,source=${PWD},destination=/var/local\" --mount \"type=bind,source=${HOME}/.aws,destination=/root/.aws,readonly\" hugo-toolbox /var/local/deploy.sh",
            "problemMatcher": []
        }
    ]
}